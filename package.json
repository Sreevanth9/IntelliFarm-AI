{
  "name": "intellifarm-ai",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  }
}
